/** Configuración de modelos disponibles para el selector de modelos del chat */

import { MODELS, CHEFS, DEFAULT_MODEL, type IModel } from '../../config/models.config'

export { MODELS, CHEFS, DEFAULT_MODEL, type IModel }
